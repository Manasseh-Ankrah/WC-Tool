const fs = require("fs");

export const getAllvalues = (bytes: number, filePath: string) => {
  console.log("====================================");
  console.log("getAllvalues RUNNING=>");
  console.log("bytes =>", bytes);
  console.log("filePath =>", bytes);
  // console.log(bytes + " " + filePath);
  console.log("====================================");
};
